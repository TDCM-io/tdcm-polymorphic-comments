New comment for
<%= link_to @commentable.find_parent, @commentable.find_parent if @commentable.is_a?(Comment) %>
<%= link_to @commentable, @commentable unless @commentable.is_a?(Comment) %>
<%= render 'comments/form', comment: @comment %>